# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Local machine paths / build configs (berisi absolute path user)
**/ephemeral/
**/xcuserdata/
**/Pods/
android/local.properties
ios/Podfile.lock
ios/.symlinks/
macos/Podfile.lock
macos/.symlinks/

# Secrets — jangan pernah commit
*.env
!.env.example
*.p8
*.p12
*.jks
*.keystore
google-services.json
GoogleService-Info.plist
firebase_app_id_file.json
key.properties

# Vercel
.vercel/

# Node backend (Vercel serverless api/) — deps installed di Vercel build
node_modules/
package-lock.json

# Drift web runtime (didownload di CI/build step, tidak perlu di repo)
web/sqlite3.wasm
web/drift_worker.js

# Logo backup lokal (bukan bagian app)
.logo_backup/
# Harness lokal
.claude/
